# ZSU Manager TODO

- [x] Trace the existing late-load root API and validate the safe trigger path for a real jailbreak action.
- [x] Move the jailbreak implementation and its validation onto a clean branch that excludes paused gaming work.
- [x] Add a guarded Home-screen Jailbreak action that appears only in a supported, unrooted late-load environment.
- [x] Add an Auto Jailbreak setting that is visible only while late-load jailbreak mode is active and persists through reboot using the supported root API.
- [x] Build and validate normal and spoofed manager variants, including supported and unsupported UI states.
- [ ] Publish the verified manager update with release notes.
